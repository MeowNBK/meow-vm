---
# Dựa trên bộ quy tắc của Google: rất sạch sẽ và nghiêm ngặt.
BasedOnStyle: Google
Language: Cpp

# --- Các tùy chỉnh chính để "thoáng" và "đẹp" hơn ---

# 1. Sử dụng 4 dấu cách để indent (Google mặc định là 2)
IndentWidth: 4
ContinuationIndentWidth: 4

# 2. Phong cách Allman: Luôn đặt dấu { ở dòng mới.
# (Google mặc định là 'Attach')
# BreakBeforeBraces: Allman

# 3. Giới hạn cột là 80 (Giống file cũ của bạn)
ColumnLimit: 100

# --- Căn chỉnh (Alignment) ---

# Căn chỉnh con trỏ/tham chiếu sang bên phải (ví dụ: int* x)
# Nếu bạn thích `int *x` (kiểu Google), đổi thành 'Left'.
PointerAlignment: Right
ReferenceAlignment: Pointer

# --- Sắp xếp Includes ---

# Tự động sắp xếp các #include
SortIncludes: CaseSensitive
IncludeBlocks: Preserve
# Phân loại include: 1. C/C++ System, 2. Thư viện ngoài, 3. Dự án của bạn
IncludeCategories:
  - Regex:           '^<.*\.h>'   # Headers C (ví dụ <stdio.h>)
    Priority:        1
  - Regex:           '^<.*>'      # Headers C++ (ví dụ <vector>)
    Priority:        2
  - Regex:           '^".*\.h"'   # Headers của dự án
    Priority:        3
  - Regex:           '.*'
    Priority:        3

# --- Các tùy chọn về dòng ngắn ---

# Giữ nguyên các tùy chọn này từ file cũ của bạn
AllowShortFunctionsOnASingleLine: All
AllowShortLambdasOnASingleLine: All
AllowShortEnumsOnASingleLine: true
AllowShortIfStatementsOnASingleLine: Never
AllowShortLoopsOnASingleLine: false

# --- Các tùy chọn về khoảng trắng (Spaces) ---

# Giống file cũ:
SpaceAfterTemplateKeyword: true
SpacesInContainerLiterals: true
SpacesInParens: Never